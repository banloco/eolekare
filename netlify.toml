[build]
  command = "npm run build"
  publish = "build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

**2. Vérifie que `public/_redirects`** contient :
```
/*    /index.html   200